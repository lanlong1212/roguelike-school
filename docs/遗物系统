# 遗物系统完整实现方案

## 一、系统概述

新增遗物（Relic）系统：精英/Boss/商店获取强力被动，每局 build 差异化。共 8 个遗物，战斗 HUD 底部显示小图标，背包和图鉴可查详情。遗物效果跨战斗持续，随存档保存。

---

## 二、遗物清单与实现细节

| ID | 名称 | 效果 | 触发位置 | 实现方式 |
|----|------|------|----------|----------|
| `light_boots` | 轻羽靴 | 每回合首次移动不耗 AP | 移动结算 | `Player` 记录 `moved_this_turn` 标记，回合开始重置 |
| `element_echo` | 元素共鸣 | 元素反应伤害 ×1.2 | 伤害结算 | `damage.py` 反应倍率后乘 1.2 |
| `war_drum` | 击战鼓 | 击杀敌人回复 1 AP | 敌人死亡结算 | 击杀时若玩家拥有则 `current_ap +1`（不超过上限） |
| `guardian_charm` | 守护符 | 每层首次受伤免疫 | 伤害结算 | `Player` 记录 `layer_guard_used` 标记，下楼重置 |
| `fire_seed` | 火种 | 火元素技能伤害 ×1.25 | 伤害结算 | 伤害计算时检查元素类型 |
| `ice_heart` | 冰心 | 被近战攻击后 50% 冻结攻击者 1 回合 | 受击结算 | 受击后随机判定，对攻击者施加冻结 |
| `giant_power` | 巨人之力 | ATK +3，最大 AP -1 | 获得/移除时 | 获取时修改属性，存档恢复时先重放 |
| `greedy_eye` | 贪婪之眼 | 击杀金币掉落翻倍 | 金币掉落结算 | 击杀时检查玩家遗物列表 |
| `echo` | 回响 | 回合结束若未移动，恢复 2 HP | 回合末结算 | 检查本回合是否移动过 |
| `decoy` | 诱饵 | 敌人优先攻击伙伴（若存活） | AI 目标选择 | 索敌时检查玩家是否拥有此遗物 |

> 注：清单里我说 8 个但列了 10 个，按你之前确认的“先做这八个”，实际取前 8 个（`light_boots` ~ `greedy_eye`）。`echo` 和 `decoy` 作为备选，后续可加。

---

## 三、掉落与获取

### 3.1 掉落规则

| 来源 | 概率 | 方式 |
|------|------|------|
| 精英怪 | 40% | 击杀后自动获得（飘字提示） |
| Boss | 100% | 击杀后自动获得 |
| 商店 | 30% | 商品列表中出现遗物位，价格 40 金 |

### 3.2 获取方式

- **战斗掉落**：敌人死亡时，若判定掉落且玩家尚未拥有该遗物，则自动加入玩家遗物列表，屏幕上方飘字“获得遗物：XXX”。
- **商店购买**：商店商品列表加入遗物，点击购买即获得。
- **重复保护**：若掉落的遗物玩家已拥有，则改为掉落 25 金币（避免无效掉落）。

---

## 四、UI 设计

### 4.1 战斗 HUD 底部遗物栏

- 位置：技能栏下方一行，每个遗物显示 24×24 小图标
- 最多显示 5 个，超过则缩小图标或显示“+N”
- 鼠标悬停显示 tooltip（名称 + 效果简述）

### 4.2 背包新增「遗物」标签页

- 按 `B` 打开背包，新增标签「遗物」
- 展示当前已获得遗物列表（图标 + 名称 + 完整效果描述）
- 未获得遗物不显示在背包中

### 4.3 图鉴新增「遗物」标签页

- 展示全部 8 个遗物
- **已获取**：正常显示图标 + 名称 + 效果
- **未获取**：涂黑图标 + “???” + “尚未获得”

---

## 五、存档集成

- 保存：玩家遗物 ID 列表 + 已获得遗物集合（用于图鉴解锁）
- 读档：恢复遗物列表，重放属性修改（防叠加，沿用天赋系统模式）
- 每层守护符状态（`layer_guard_used`）随存档保存

---

## 六、文件改动清单

| 文件 | 改动 |
|------|------|
| **新增** `src/items/relics.py` | 遗物定义（ID、名称、效果、图标映射） |
| **修改** `src/entities/player.py` | 添加 `relics: list[str]`、`owned_relics: set[str]`、`moved_this_turn`、`layer_guard_used` |
| **修改** `src/combat/damage.py` | 元素共鸣、火种、守护符、冰心触发 |
| **修改** `src/combat/battle_manager.py` | 击杀回AP、回响、金币翻倍、掉落判定 |
| **修改** `src/world/dungeon_generator.py` 或房间逻辑 | Boss/精英击杀后掉落遗物 |
| **修改** `src/ui/menu.py` | 商店遗物、背包遗物标签页 |
| **修改** `src/ui/help_panel.py` | 图鉴加遗物页（涂黑逻辑） |
| **修改** `src/states/play_state.py` | HUD 遗物栏 + tooltip |
| **修改** `src/core/save_manager.py` | 遗物存档/读档 |
| **修改** `src/ai/behaviors/` | 诱饵（若包含）—— 本次不实现 |

---

## 七、开发步骤

### 步骤 1：数据层（relics.py + Player 扩展）
- 定义 8 个遗物数据（ID、名称、描述、图标）
- Player 添加遗物相关属性

### 步骤 2：掉落系统
- 精英/Boss 击杀掉落逻辑
- 商店遗物位

### 步骤 3：效果触发
- 逐项实现 8 个遗物的被动效果

### 步骤 4：UI（HUD 栏 + 背包 + 图鉴）
- 战斗 HUD 遗物栏
- 背包遗物标签页
- 图鉴遗物页（涂黑）

### 步骤 5：存档集成与测试
- 遗物存档/读档
- 跑测试

