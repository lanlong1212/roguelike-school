# Roguelike 回合制策略游戏

基于 pygame-ce 开发的 Roguelike 回合制策略游戏。

## 环境准备

```powershell
# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境（PowerShell）
.venv\Scripts\Activate.ps1

# 安装依赖
pip install -r requirements.txt
```

## 启动游戏

```powershell
.venv\Scripts\python.exe src\main.py
```
